Ext.namespace('xl.debug');

/* xl.debug.window = new Ext.Window({
	title: 'Debug Console',
	width: 500,
	height: 400,
	minWidth: 300,
	minHeight: 200,
	resizable: false,
	renderTo: document.body,
	
	layout: 'fit',
	bodyStyle: 'padding: 8px;',
	
	buttonAlign: 'right',
	buttons: [{ text: 'Dismiss', handler: function() {xl.debug.window.hide();} }],
	
	items: new Ext.form.FormPanel({
		baseCls: 'x-plain',
		items: [{
			xtype: 'textarea',
			hideLabel: true,
			name: 'xl.debug.textarea',
			frame: false,
			autoScroll: true,
			anchor: '90% 100%'
		}]
	})
}); */

//xl.debug.window.setPosition(100,100);
//xl.debug.window.show();