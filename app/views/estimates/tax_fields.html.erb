<% inline_remote_form_for :estimate, @estimate, :url => estimate_path(@estimate), :html => {:method => :put, :id => (dom_id(@estimate) + "_tax_fields")} do |f| %>
  <%= render :partial => "tax_fields", :locals => {:f => f} %>
<% end %>
<%= javascript_tag %Q`
  new XlSuite.FormHandler("#{dom_id(@estimate)}_tax_fields", {now:true, defaultValue:"N/A"});
`%>